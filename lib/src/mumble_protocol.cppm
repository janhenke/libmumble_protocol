//
// Created by JanHe on 23.05.2024.
//

export module mumble_protocol;

export import :client;
export import :server;

// exported for now to enable unit testing, should actually be in an internal partition
export import :utility;
